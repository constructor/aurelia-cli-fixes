{
  "name": "dot-new",
  "description": "Creates a new Aurelia application project from a dotnet custom template.",
  "parameters": [
    {
      "name": "template-name",
      "optional": false,
      "description": "The name of the template to create the project with."
    }
  ],
  "flags": [
    {
      "name": "here",
      "description": "Creates a new project with the current working directory as the root of the project instead of creating a new project folder.",
      "type": "boolean"
    }
  ]
}
